{
  "name": "steel-front-douyin",
  "version": "0.1.0",
  "private": true,
  "description": "钢铁前线 - 原创即时战略抖音小游戏（单机）",
  "scripts": {
    "test": "node --test --test-force-exit",
    "check-size": "node scripts/check-size.js",
    "audit-dom": "node scripts/audit-dom.js",
    "quality-gate": "node scripts/quality-gate.js",
    "build": "node scripts/build.js",
    "format": "prettier --write adapter game scripts tests game.js",
    "format:check": "prettier --check adapter game scripts tests game.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "prettier": "^3.3.3"
  },
  "engines": {
    "node": ">=18"
  }
}
